<div class="container mt-3">
  <h1>My Tasks</h1>
    <% @tasks.each do |task|%>
      <li><%= link_to task.title, task_path(task), class: 'text-primary text-decoration-none'%></li>
    <% end %>
  <br>
  <%= link_to "Add a new task", new_task_path, class:"btn btn-primary" %>
</div>
